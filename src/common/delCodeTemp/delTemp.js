{
  /* <ReactPlayer
                    className="remote-video"
                    url={stream && stream.mediaStream}
                    width="100%"
                    height="100%"
                    playing
                    playsinline
                  /> */
}
{
  /* <div className="video-info">
                    <span className="user-name">{stream.uid}</span>
                    <span
                      className={`volume-wrapper ${muteRemoteVolume.map((e) => {
                        if (e.sid === stream.sid) {
                          return "mute-audio";
                        }
                      })}`}
                    >
                      <span className="icon__urtc-mkf"></span>
                      {remoteVolumeData.map((data, i) => (
                        <span
                          className="volume-data"
                          key={i}
                          style={{
                            top:
                              data.sid === stream.sid
                                ? 14 - Math.ceil((data.volume / 100) * 8) + "px"
                                : "14px",
                          }}
                        ></span>
                      ))}
                    </span>
                  </div>
                 */
}
